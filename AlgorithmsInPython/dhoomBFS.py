from collections import dequeue
ikey,lvalue=input("").strip().split(" ")
ikey,lvalue=int(ikey),int(lvalue)
nkey=int(input(""))
keys=[int(n) for n in input("").strip().split(" ")]
#multipliedKey=dequeue()
multipliedKey=list()
inlcudeKey=list() # to check whether key is multiplied
# inlcudeKey[0]=True
# for i in range(nkey):
# 	inlcudeKey.append(False)
time=0
while(lvalue not in multipliedKey[time]):
	l=list()
	for n in keys:
		l.append()
